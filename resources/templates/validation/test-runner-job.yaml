apiVersion: batch/v1
kind: Job
metadata:
  name: test-runner-{{ .Values.environment }}
  namespace: {{ .Values.namespace }}
  labels:
    type: host
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      serviceAccountName: {{ .Values.serviceAccount }}
      restartPolicy: Never
      containers:
        - name: test-runner-container
          image: us.icr.io/cloudrock_cicd/test-runner:stable
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: {{ .Values.testRunnerSecret }}
          resources:
            limits:
              cpu: "500m"
            requests:
              cpu: "250m"
  parallelism: 1
  backoffLimit: 0